# 04 — 环图标签黑色合规

标签: 已完成（2026-08-30）

## 修复记录

- `plot_docking_ring_heatmap.py` 中心副标题 `#333333`→`#000000`、缺图占位 `#666666`→`#000000`；连接线 `#888888`/hub 描边 `#444444` 为图形元素非文字标签，保留。
- 新增 `测试_tests/test_ring_heatmap_label_color.py`：扫描全部 `ax.text` 块，锁定文字标签必须 `#000000`（pytest 5 passed）。
- E2E：临时项目（canonical summary_vina.csv，5 任务）重跑，PNG+SVG 均重新生成并目检通过；`杨程茗分子对接` 原目录已不在桌面，待恢复后可一键重跑出图。

## 来源

Spec 轴（c），SOP §6「标签一律黑色 #000000」。

## 问题

`plot_docking_ring_heatmap.py:297` 中心副标题 `#333333`；`:261` 缺图占位 `#666666`。

## 验收标准

- 两处改 `#000000`；重跑 `杨程茗分子对接` 环图，目检标签全黑；
- PNG+SVG 均更新。
