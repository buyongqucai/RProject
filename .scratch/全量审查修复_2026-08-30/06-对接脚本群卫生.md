# 06 — 对接脚本群卫生

标签: 已完成（2026-08-30）

## 修复记录

- 新增 `dock_export_common.py`：`log` / `ensure_img_subdir` / `collapse_console_qt` 三公共 helper；`pymol_dock_viz_standard.py`、`plot_docking_ring_heatmap.py`、`export_detail_png_from_pse.py`、`pymol_detail_export_hook.py`、`collapse_pymol_console.py`、`run_nuli_ting_detail_combine.py` 六处改为引用，消除清单 1–3 重复。
- `collapse_pymol_console.py`：删 `SCRIPT_DIR` 死变量；docstring 去掉 PyMOL 命令行 `run` 用法，只留 `-r`。
- `pymol_dock_viz_standard.py` 头注释 `label=0` → `label=1`（与实现一致）；删 select_jobs 内重复 sys.path.insert。
- `run_nuli_ting_detail_combine.py`：`result_01.png` 跳过/删除各补一句意图注释；删未用 `tempfile`；硬编码路径归入文件头「项目专用路径常量」区块并注明。`collect_nuli_viz_top10.py` 同样加「项目专用」标注。
- `export_detail_png_from_pse.py` 删未用 `shutil`。
- 验证：pytest 5 passed；4 个脚本 import smoke + hook/collapse 语法检查 OK。

## 来源

Standards 轴（Duplicated Code 等判断性坏味道）。

## 问题清单

1. `log()` 在 `pymol_dock_viz_standard.py:31` 与 `plot_docking_ring_heatmap.py:50` 逐字重复；
2. `_collapse_console()`（hook:51-103）≈ `_collapse()`（`collapse_pymol_console.py:19-55`）；
3. `normalize_job()`（export:136-151）≈ `ensure_img_subdir()`（`run_nuli_ting_detail_combine.py:51-66`）；
4. `collapse_pymol_console.py:17` `SCRIPT_DIR` 死变量；docstring 提供 PyMOL 命令行 `run` 用法（与文档相悖）；
5. `pymol_dock_viz_standard.py:6` 头注释 `label=0` 陈旧（实为 `label=1`）；
6. `run_nuli_ting_detail_combine.py:101,133` 无注释跳过/删除 `result_01.png`；
7. `collect_nuli_viz_top10.py:16-19`、`run_nuli_ting_detail_combine.py:16-24` 硬编码桌面项目路径（SOP 已点名项目专用，部分豁免）。

## 验收标准

- 抽公共 helper（如 `脚本_scripts/dock_export_common.py`）消除 1–3 重复；
- 删除死变量、更新陈旧注释与 docstring；
- `result_01.png` 跳过逻辑补一句非显然意图注释；
- 硬编码路径集中为文件头常量并注明「项目专用」。
