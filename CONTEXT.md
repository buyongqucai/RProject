# CONTEXT — RProject 领域词汇与边界

> 单上下文 SSOT（见 `docs/agents/领域文档_domain.md`）。新术语/边界决策落地即更新本文件；架构级决策另建 `docs/架构决策_ADR/`。

## 项目定位

个人生信技能库 + 项目交付仓库。核心资产：`生信分析技能_BioinformaticsSkills/`（技能 = 文档 + 脚本 + 样例）。

## 领域词汇

| 术语 | 含义 |
|------|------|
| 技能（skill） | `生信分析技能_BioinformaticsSkills/<类别>/<名称>/` 下的完整单元：`技能说明_*.md`（入口）+ `脚本_scripts/` + `01_样例_sample/` + `文档_docs/` |
| 技能说明 | 技能的必读入口文档；agent 动手前必须先 Read（路由规则强制） |
| SOP | `文档_docs/` 内的流水线细则，技能内最高执行优先级 |
| FROZEN | 已冻结的交付样式（如网药图轨、MD Origin/报告范式），未经用户「解冻」禁止改动 |
| 金标 | 交付目录的参照样板（如桌面 `痤疮_分子对接_序号文件夹/`） |
| SSOT | 单一事实来源；同一含义只在一处维护 |
| 接缝（seam） | 测试的公开边界；TDD 前须与用户书面确认 |
| 红绿循环 | 先写失败测试（红）再最小实现（绿）；重构不属于循环 |
| 双轴审查 | code-review 的 Standards（规范）+ Spec（规格）两轴，分开报告不合并排名 |
| 契约桩（BLOCKED） | 无真实数据时的合规占位样例；provenance 必须标 BLOCKED，禁止标 toy |

## 边界

- 技能库脚本服务**所有**项目，禁止写死单一项目路径（项目专用脚本须在文件头注明）。
- 交付项目（如桌面 `杨程茗分子对接`）独立于技能库管理，各有自己的 git。
- 医学/生物学/学术口径见 `docs/项目规范_ProjectStandards.md`。
